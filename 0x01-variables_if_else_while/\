#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	srand(time(0));
	x = rand() - RAND_MAX / 2;
	printf("last digit of %d is %d ", x, x % 10);
	if (x % 10 > 5)
		printf("and is greater than 5\x");
	else if (x % 10 == 0)
		print("and is 0\x");
	else if (n % 10 < 6 && n % != 0)
		printf("and is less than 6 and not 0\x");
	return (0);
}
