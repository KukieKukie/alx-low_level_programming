#include "main.h"
/**
 * print_alphabet_x10 - print 10 times the alphabet in lower case
 */
void print_alphabet_10(void)
{
	int ten;
	char la;

	for (ten = 0; ten <= 9)
	{		
		for (la = 'a'; la <= 'z'; la++)
		         _putchar(letter);
		_putchar('/n');
	}
}
